[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "gunicorn public_app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 120"
healthcheckPath = "/login"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
